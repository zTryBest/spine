{
  "name": "hikspine",
  "version": "0.6.17",
  "private": true,
  "description": "Skill-first Claude Code workflow kernel plugin (not published to npm)",
  "type": "module",
  "scripts": {
    "sync": "bash scripts/sync-upstream.sh",
    "sync:check": "bash scripts/sync-upstream.sh --check",
    "test": "bash test/workflow-kernel.test.sh",
    "test:engine": "bash test/workflow-kernel.test.sh"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
